---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: magnumdeployments.magnum.yaook.cloud
spec:
  group: magnum.yaook.cloud
  names:
    categories:
    - yaook
    - openstack
    kind: MagnumDeployment
    listKind: MagnumDeploymentList
    plural: magnumdeployments
    shortNames:
    - magnum
    - magnumdeploy
    singular: magnumdeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.targetRelease
      name: Release
      type: string
    - jsonPath: .spec.api.ingress.fqdn
      name: FQDN
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          MagnumDeployment deploys the OpenStack Container Infrastructure Management
          service (Magnum) on top of a Yaook-managed OpenStack control plane.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MagnumDeploymentSpec defines the desired state of a Magnum
              deployment.
            properties:
              api:
                description: APISpec configures the magnum-api deployment.
                properties:
                  ingress:
                    description: IngressSpec mirrors the ingress block used by the
                      upstream Yaook operators.
                    properties:
                      createIngress:
                        default: true
                        description: CreateIngress controls whether the Ingress object
                          is created at all.
                        type: boolean
                      externalCertificateSecretRef:
                        description: |-
                          ExternalCertificateSecretRef points at an existing TLS secret (for example
                          one issued by cert-manager from a public ACME issuer). When unset the
                          Ingress is served with the certificate issued by IssuerRef.
                        properties:
                          name:
                            minLength: 1
                            type: string
                        required:
                        - name
                        type: object
                      fqdn:
                        description: FQDN is the outer fully-qualified domain name
                          of the Ingress.
                        minLength: 1
                        type: string
                      ingressClassName:
                        default: nginx
                        description: IngressClassName selects the ingress controller.
                        type: string
                      port:
                        description: |-
                          Port is the port the Ingress is reachable on. It is required to build the
                          public KeystoneEndpoint URL, which may differ from the container port.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    required:
                    - fqdn
                    - port
                    type: object
                  replicas:
                    default: 1
                    format: int32
                    minimum: 1
                    type: integer
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                required:
                - ingress
                type: object
              conductor:
                description: ConductorSpec configures the magnum-conductor deployment.
                properties:
                  replicas:
                    default: 1
                    format: int32
                    minimum: 1
                    type: integer
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              database:
                description: |-
                  DatabaseSpec configures the MariaDB instance provisioned via the Yaook
                  infra-operator (infra.yaook.cloud/v1 MySQLService).
                properties:
                  backupSchedule:
                    default: 0 * * * *
                    description: BackupSchedule is a crontab expression for database
                      backups.
                    type: string
                  proxyReplicas:
                    default: 1
                    description: ProxyReplicas sets the number of MySQL proxy replicas.
                    format: int32
                    minimum: 1
                    type: integer
                  replicas:
                    default: 1
                    format: int32
                    minimum: 1
                    type: integer
                  storageClassName:
                    minLength: 1
                    type: string
                  storageSize:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 2Gi
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  targetRelease:
                    default: "11.4"
                    description: TargetRelease is the MariaDB release provisioned
                      by the infra-operator.
                    type: string
                required:
                - storageClassName
                type: object
              images:
                description: |-
                  ImagesSpec allows overriding the container images used for Magnum.

                  Yaook's own registry does not publish Magnum images, so the defaults point at
                  the OpenStack Kolla images on quay.io.
                properties:
                  api:
                    type: string
                  conductor:
                    type: string
                type: object
              issuerRef:
                description: |-
                  IssuerRef is the cert-manager Issuer used for internal TLS, matching the
                  issuerRef used by the upstream Yaook deployments.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              keystoneRef:
                description: KeystoneRef points at the KeystoneDeployment this service
                  authenticates against.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              magnumConfig:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  MagnumConfig is merged into the generated magnum.conf. Outer keys are
                  section names, inner keys are options. Values set here win over the
                  operator's generated defaults, except for credentials.
                type: object
              messageQueue:
                description: |-
                  MessageQueueSpec configures the RabbitMQ instance provisioned via the Yaook
                  infra-operator (infra.yaook.cloud/v1 AMQPServer).
                properties:
                  replicas:
                    default: 1
                    format: int32
                    minimum: 1
                    type: integer
                  storageClassName:
                    minLength: 1
                    type: string
                  storageSize:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 2Gi
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  targetRelease:
                    default: "4.2"
                    description: TargetRelease is the RabbitMQ release provisioned
                      by the infra-operator.
                    type: string
                required:
                - storageClassName
                type: object
              region:
                description: RegionSpec identifies the OpenStack region the service
                  is registered in.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              targetRelease:
                default: "2025.1"
                description: TargetRelease is the OpenStack release to deploy, e.g.
                  "2025.1".
                type: string
            required:
            - api
            - database
            - issuerRef
            - keystoneRef
            - messageQueue
            - region
            type: object
          status:
            description: MagnumDeploymentStatus reports the observed state of a Magnum
              deployment.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
